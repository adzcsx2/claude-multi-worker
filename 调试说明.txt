===============================================
调试指南
===============================================

问题: 只启动了一个窗格，标题没有改名

已添加的调试信息:
1. [DEBUG] Created pane: <pane_id>
2. [DEBUG] Current pane ID: <pane_id>  
3. [DEBUG] Set title for pane: <title>
4. [DEBUG] Failed to set title: <error>

===============================================
测试步骤:
===============================================

1. 先运行测试脚本验证 WezTerm CLI:
   python test_wezterm_cli.py
   
   这会测试:
   - 列出窗格
   - 创建新窗格
   - 发送命令到窗格
   - 设置窗格标题

2. 如果测试通过，运行实际启动:
   python cms.py claude:ui,coder
   
3. 查看调试输出:
   - 检查是否显示 [DEBUG] Created pane
   - 检查是否显示 [DEBUG] Set title

===============================================
可能的问题:
===============================================

1. split-pane 失败
   → 检查是否在 WezTerm 中运行
   → 检查 WezTerm 版本

2. 标题设置失败
   → set-tab-title 可能需要不同的参数
   → 可能需要使用不同的命令

3. pane_id 格式问题
   → 确认返回的 pane_id 格式正确

===============================================
立即测试:
===============================================

在 WezTerm 中运行:
  cd E:\ai_project\claude-multi-starter
  python test_wezterm_cli.py

查看输出，确认哪一步失败
===============================================
